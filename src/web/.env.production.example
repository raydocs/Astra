# Copy to src/web/.env.production for a real Cloudflare Pages production build.
# Replace placeholders with verified public origins before deploying.
# Blocker as of 2026-05-22: final public web/API domains are not recorded in-repo.
VITE_ASTRA_API_BASE_URL=https://relay-lite.example.invalid/v1
VITE_ASTRA_PLATFORM_BASE_URL=https://platform-or-relay-lite.example.invalid/v1
