# Learning-loop UX debt (Month 2 backlog)

- Unify English/Chinese hard strings in `ReviewMode` / `VocabularyApp` list chrome with `_locales` (incremental i18n pass).
- Add “open in popup study” when deep-linking from a saved word (sentence index contract).
- Stronger empty-state when **no** `studyContext` but history exists (copy + CTA to open last page).
- Daily stats tooltip explaining reset boundary (local midnight vs UTC).
